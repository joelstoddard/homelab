---
# Every `alloy` host is reached as root: the NUCs already are via the
# proxmox group var, and the Pi-hole LXC is a tofu-built Debian container
# whose only account is root. This can't live in the SOPS file next to it —
# sops vars only load at task stage, too late for a connection var.
ansible_user: root
