# Proxmox VE's UI, on 8006 with the self-signed certificate every node ships.
apiVersion: v1
kind: Service
metadata:
  name: rumba
spec:
  clusterIP: None
  ports:
    - name: https
      port: 8006
      targetPort: 8006
---
apiVersion: discovery.k8s.io/v1
kind: EndpointSlice
metadata:
  name: rumba
  labels:
    kubernetes.io/service-name: rumba
addressType: IPv4
ports:
  - name: https
    port: 8006
endpoints:
  - addresses:
      - ${RUMBA_IP}
    conditions:
      ready: true
---
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: rumba
spec:
  entryPoints:
    - websecure
  routes:
    - kind: Rule
      match: Host(`rumba.${DOMAIN}`)
      middlewares:
        - name: default-headers
          namespace: traefik
      services:
        - name: rumba
          port: 8006
          scheme: https
          serversTransport: traefik-lan-insecure@kubernetescrd
