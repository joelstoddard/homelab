# The Alloy chart (alloy-gateway below) lives in grafana/helm-charts, not
# grafana-community; same repository the alloy layer uses.
apiVersion: source.toolkit.fluxcd.io/v1
kind: HelmRepository
metadata:
  name: grafana
spec:
  interval: 1h0m0s
  url: https://grafana.github.io/helm-charts
