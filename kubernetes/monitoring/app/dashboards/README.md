# Dashboards

Every file here becomes one ConfigMap (kustomization.yaml), labelled
`grafana_dashboard: "1"`, and Grafana's sidecar loads it into the folder named
by its `grafana_folder` annotation. Provisioned dashboards are read-only in
the UI: Save As a copy, iterate, Export → JSON model with "export for sharing
externally" **off**, drop the file in its folder directory, add a generator
entry, PR. The copy disappears on the next Grafana restart.

Vendoring: `python3 vendor.py <Folder>/<name>.json --id <grafana.com id>`
pins every datasource reference to the provisioned UIDs (`prometheus`,
`loki`) and strips `__inputs`. Datasources with other UIDs will show
"datasource not found". The Cilium dashboards are not here: the Cilium chart
ships them (`dashboards.enabled` in `kubernetes/cilium/app/values.yaml`) and
the sidecar watches every namespace.

| File | Folder | Source | Revision | Local edits |
| --- | --- | --- | --- | --- |
| Kubernetes/k8s-views-global.json | Kubernetes | grafana.com/grafana/dashboards/15757 | 43 | datasource UIDs pinned |
| Kubernetes/k8s-views-namespaces.json | Kubernetes | grafana.com/grafana/dashboards/15758 | 46 | datasource UIDs pinned |
| Kubernetes/k8s-views-nodes.json | Kubernetes | grafana.com/grafana/dashboards/15759 | 40 | datasource UIDs pinned |
| Kubernetes/k8s-views-pods.json | Kubernetes | grafana.com/grafana/dashboards/15760 | 39 | datasource UIDs pinned |
| Kubernetes/node-exporter-full.json | Kubernetes | grafana.com/grafana/dashboards/1860 | 45 | datasource UIDs pinned |
| Storage/longhorn.json | Storage | grafana.com/grafana/dashboards/13032 | 6 | datasource UIDs pinned |
| Ingress/traefik.json | Ingress | grafana.com/grafana/dashboards/17346 | 9 | datasource UIDs pinned |
| Ingress/cert-manager.json | Ingress | grafana.com/grafana/dashboards/20842 | 3 | datasource UIDs pinned |
| Flux/control-plane.json | Flux | fluxcd/flux2-monitoring-example monitoring/configs/dashboards/control-plane.json | main @ 2026-09 | datasource UIDs pinned |
| Logs/logs-explorer.json | Logs | hand-written | — | — |
| Logs/kubernetes-events.json | Logs | hand-written | — | — |
| Tracing/beyla-red.json | Tracing | grafana.com/grafana/dashboards/19923 | 3 | datasource UIDs pinned |
| Hosts/proxmox.json | Hosts | grafana.com/grafana/dashboards/10347 | 5 | datasource UIDs pinned |
| Hosts/pihole.json | Hosts | grafana.com/grafana/dashboards/10176 | 3 | datasource UIDs pinned; cleared a leftover LAN IP default from the `node` template variable; the `hostname` label it lists is rewritten to `pihole` by the gateway's relabel — the exporter would otherwise emit the address |
| Hosts/blackbox.json | Hosts | grafana.com/grafana/dashboards/7587 | 3 | datasource UIDs pinned |
| Hosts/host-logs.json | Hosts | hand-written | — | — |
| Services/searxng.json | Services | hand-written | — | — |
| Services/tailscale.json | Services | hand-written | — | tailscaled's built-in metrics only; the traffic counters read zero until a peer actually sends. kube-state-metrics series are matched on `exported_namespace`, not `namespace` |
| Services/jellyfin.json | Services | hand-written | — | Jellyfin exposes no Prometheus endpoint, so request panels come from Beyla, matched on `k8s_namespace_name` — Beyla stamps `namespace` with its own — plus `k8s_deployment_name`, since `media` is a shared namespace and every panel here filters to the Jellyfin workload. kube-state-metrics series use `exported_namespace`, as tailscale's do. Longhorn robustness is `max()` because every manager reports the volume and only its owner knows the real state. Playback panels need the `jellyfin-exporter` Deployment and its API key; they read empty until that secret is populated |
| Services/glance.json | Services | hand-written | — | Glance exposes no Prometheus endpoint and has no volume, so request panels come from Beyla, matched on `k8s_namespace_name` as Jellyfin's are; kube-state-metrics series use `exported_namespace`. Expect long flat stretches — a start page is opened rarely. Sustained 4xx is the `glance-auth` middleware refusing, not Glance |
| Services/home-assistant.json | Services | hand-written | — | Home Assistant exposes its own Prometheus endpoint at `/api/prometheus`, scraped by an explicit Alloy job authenticated with a bearer token — that feeds the entity-level panels. Beyla can never fill the request-panel gap: `containers_only: true` compares a process's network namespace against Beyla's own, and both Beyla and this pod run `hostNetwork: true`, so the process reads as a host process, not a container, forever. Request panels come from Traefik's per-service metrics instead, matched with a `service=~"home-assistant-home-assistant-.*"` regex because the Kubernetes CRD provider's hash suffix can change; the tradeoff is that Traefik only sees traffic through the ingress, so requests to the pinned LoadBalancer address and in-cluster probes never show up. kube-state-metrics series use `exported_namespace`. No network throughput panel: the pod runs `hostNetwork: true`, so `container_network_*` enumerates every interface on the node and would report the node's whole overlay traffic as Home Assistant's — omitted rather than shown misleading. The entity count panels use `count(...) or vector(0)` deliberately: without the `vector(0)` fallback a fully healthy instance reads "No data" instead of 0, which is backwards for an at-a-glance health panel |
| Services/prowlarr.json | Services | hand-written | — | hand-written; one per exportarr-backed service, sharing a health / resources / logs skeleton. Three label spellings appear in each: kube-state-metrics on `exported_namespace`, cAdvisor and kubelet on bare `namespace`, and the exporter's own series unqualified. `up{job="<app>-exporter"}` is the exporter health signal — in v2 a failing collector 500s the whole scrape and `<app>_collector_error` is never exported. Query counters start from zero when the exporter starts, not from Prowlarr's history — its collector reads a stats endpoint. `Unavailable` needs `or vector(0)`: a healthy Prowlarr emits no series at all |
| Services/sonarr.json | Services | hand-written | — | hand-written; one per exportarr-backed service, sharing a health / resources / logs skeleton. Three label spellings appear in each: kube-state-metrics on `exported_namespace`, cAdvisor and kubelet on bare `namespace`, and the exporter's own series unqualified. `up{job="<app>-exporter"}` is the exporter health signal — in v2 a failing collector 500s the whole scrape and `<app>_collector_error` is never exported. `Download queue` needs `or vector(0)` — an empty queue emits no series in v2, so without it an idle Sonarr reads No data instead of 0 |
| Services/radarr.json | Services | hand-written | — | hand-written; one per exportarr-backed service, sharing a health / resources / logs skeleton. Three label spellings appear in each: kube-state-metrics on `exported_namespace`, cAdvisor and kubelet on bare `namespace`, and the exporter's own series unqualified. `up{job="<app>-exporter"}` is the exporter health signal — in v2 a failing collector 500s the whole scrape and `<app>_collector_error` is never exported. `By quality` is empty on an empty library; that series only exists for qualities actually present |
| Services/lidarr.json | Services | hand-written | — | hand-written; one per exportarr-backed service, sharing a health / resources / logs skeleton. Three label spellings appear in each: kube-state-metrics on `exported_namespace`, cAdvisor and kubelet on bare `namespace`, and the exporter's own series unqualified. `up{job="<app>-exporter"}` is the exporter health signal — in v2 a failing collector 500s the whole scrape and `<app>_collector_error` is never exported. Genres are artist genres only — album genres and song qualities need `ENABLE_ADDITIONAL_METRICS`, which is off. `By genre` is emitted per genre actually recorded, so it reads empty until an artist carries one; no `vector(0)` fallback, which would invent a series with no genre label |
| Services/bazarr.json | Services | hand-written | — | hand-written; one per exportarr-backed service, sharing a health / resources / logs skeleton. Three label spellings appear in each: kube-state-metrics on `exported_namespace`, cAdvisor and kubelet on bare `namespace`, and the exporter's own series unqualified. `up{job="<app>-exporter"}` is the exporter health signal — in v2 a failing collector 500s the whole scrape and `<app>_collector_error` is never exported. This exporter's scrape is the slow one and will outgrow the annotation path's 10s timeout on a large library, which shows here as `up == 0` with a healthy pod — measured at 1.7s against a movies-only Bazarr on 2026-09-22. The `episode_subtitles_*` family is not emitted at all until Bazarr manages a TV series, so those two panels carry `or vector(0)` |
| Services/sabnzbd.json | Services | hand-written | — | hand-written; one per exportarr-backed service, sharing a health / resources / logs skeleton. Three label spellings appear in each: kube-state-metrics on `exported_namespace`, cAdvisor and kubelet on bare `namespace`, and the exporter's own series unqualified. `up{job="<app>-exporter"}` is the exporter health signal — in v2 a failing collector 500s the whole scrape and `<app>_collector_error` is never exported. Not an \*arr, but exportarr speaks it and it lives in this namespace. Disk figures are per write folder, both on the same NFS export here |
