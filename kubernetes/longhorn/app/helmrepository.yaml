# Longhorn publishes a classic Helm repo, not an OCI chart (unlike Cilium),
# hence HelmRepository + chart.spec rather than OCIRepository + chartRef.
apiVersion: source.toolkit.fluxcd.io/v1
kind: HelmRepository
metadata:
  name: longhorn
spec:
  interval: 1h0m0s
  url: https://charts.longhorn.io
