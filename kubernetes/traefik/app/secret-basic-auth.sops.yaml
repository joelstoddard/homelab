# htpasswd lines for the basic-auth Middleware. bcrypt only (htpasswd -nB) —
# see middlewares.yaml.
apiVersion: v1
kind: Secret
metadata:
  name: basic-auth-users
  namespace: traefik
type: Opaque
stringData:
  users: REPLACE_ME
