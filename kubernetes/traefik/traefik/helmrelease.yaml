apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: traefik
  namespace: traefik
spec:
  chart:
    spec:
      chart: traefik
      reconcileStrategy: Revision
      sourceRef:
        kind: HelmRepository
        name: traefik
      valuesFiles:
        - values.yaml
      version: 29.0.1
  interval: 15m0s
